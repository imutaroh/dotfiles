---
name: research-to-note
description: 指示を受けて裏取りリサーチを行い、その結果を note の"大バズ記事"に近い読みやすいフォーマット（短段落・小見出し頻度・冒頭フック・画像配置間隔）に整形して Company/Drafts/ に投入するパイプラインSkill。画像は本文中に機械可読なプレースホルダ（image-slotコメント＋日本語生成プロンプト）で埋め込み、後からAPI連携でURL差し替えできる状態にする。「これリサーチしてnoteドラフトにして」「バズフォーマットでまとめて」「リサーチから記事まで一気に」「調べてnote形式にして」で使用。内蔵リサーチ手順（旧researchスキル継承）・note-createの手法と内蔵の掴み採点（5フックタイプ）を1本に連結するオーケストレータ。
---

# リサーチ→noteドラフト（research-to-note）

ネタ・問いを1つ受け取り、**リサーチ→掴み→note大バズ風フォーマット整形→画像プレースホルダ付き保存**まで一気に走らせるパイプライン。各工程は既存Skillの手法を踏襲し、このSkillは「連結」と「note大バズフォーマットへの整形」だけを担う。

> [!warning] 再実装しない
> リサーチ手順は Step 1 に内蔵（旧 `research` スキルは 2026-07-27 に削除済み。手法はこのSkillに内蔵）。noteの文体・黄金比率は `note-create` に準拠する。タイトル/書き出しの量産・採点は Step 2 に内蔵の5フックタイプ手順を使う（旧 `hook-and-headline` は2026-07-17に削除済み。手法はこのSkillに内蔵）。このSkill固有の仕事は「フォーマット整形」「掴み採点」「画像プレースホルダ規約」の3つ。

## 前提（このVaultでの作法）

- **出力先**: `Company/Drafts/YYYY-MM-DD-slug.md`（1案件=1ノート）
- **書き込み禁止**: `Vault/` 配下（読むだけ）
- **スタイル規則本体**: `references/style-guide.md`（Step 2 で必ず読む）

## パイプライン

```
Step 1  リサーチ（内蔵手順）
Step 2  掴み（内蔵の5フックタイプ採点）：タイトル・書き出し
Step 3  本文整形（style-guide.md のnote大バズフォーマット＋note-create の黄金比率）
Step 4  画像プレースホルダ埋め込み
Step 5  Company/Drafts/ に保存・完了報告
```

## Step 1: リサーチ

以下の内蔵手順で裏取りする（旧 `research` スキルの6ステップを継承）: **目的確認 → 調査軸の提示 → WebSearch 5本並列 → 不足分の補完検索 → 出典付きレポート化**。出典は必ず記録する（Step 3以降で本文に引用するため）。
- 記事の主張が重い・論争的で敵対的な裏取りが要る場合は、Step 1 の検索本数を増やし、反証側の出典も明示的に探す。
- 体験談ベースで裏取り不要と判断できる場合はスキップしてよいが、スキップした旨を完了報告に残す。
- 裏取りの線引き：**主観の吐露（自分の感情・体験の描写）＝出典不要／統計・理論・固有名詞・社会的主張＝出典必須**。

## Step 2: 掴み

5フックタイプ（問題提起／好奇心／社会的証明／物語／直接便益）でタイトル候補を複数出し、診断表（具体性・自分ごと化・情報ギャップ・媒体適合・guru臭の有無）で1案に絞る。書き出しも同様に3案→1案。
- note媒体なので **タイトル40字以内**、**冒頭50字で「これは何か」を言い切る**（style-guide.md §1）ことを条件に加える。

## Step 3: 本文整形（このSkillの核）

`references/style-guide.md` を読み、以下を満たす形に本文を組む：

1. 冒頭：概要文50字＋数字1つ＋箇条書き要点3〜4個
2. 段落：2〜3文で改行、5行以上は禁止
3. 小見出し：200〜400字に1つ、20字以内
4. 太字：1段落1箇所まで
5. 文体：`note-create` の黄金比率（断定5：推量2以下：疑問1〜2：体言止め2〜3／10文あたり目安）
6. listicleに着地させない。骨格はフォーマットで作るが、肉付けは具体シーン・地の文で行う。**小見出し密度は目安であり、地の文の連続性を優先**（密度だけ守ると「小見出し＋2段落」の羅列＝listicle感が出る）
7. リサーチ由来の主張には出典を明示する（本文内リンク or 末尾「## 参考」節）

## Step 4: 画像プレースホルダ埋め込み

小見出し2〜3個（=3〜5スクロール目安、style-guide.md §6）ごとに1箇所、本文中に以下の3点セットを挿入する：

```markdown
<!-- image-slot: 01-{内容が分かる短いslug} -->
![（この図が何を表すかの短い代替テキスト）](TODO-image-url)
<!-- image-prompt(ja): ここに article-visual-planner 流儀の日本語画像生成プロンプトを書く。
     STYLE: クリーンなフラット図解、日本語ラベルは画像内に直接、装飾文字なし
            デフォルト配色: クリーム地＋濃紺＋ティール/オレンジ（既存図解と統一）
     SCENE: 何を・どう配置するかを具体的に -->
```

- 連番は `01`, `02`... で通し番号にする（機械的に検索・差し替えできるように）
- `TODO-image-url` は固定文字列にする（あとでAPI連携がこの文字列を検索して置換できるように）
- 画像プロンプトの中身は `article-visual-planner` の流儀（フラットSTYLE・日本語ラベル直書き・実データを入れた「図解」であり「挿絵」にしない）に従う
- 図が不要な短い記事では0箇所でもよい（枚数を稼がない）
- **短尺（1500〜2000字）の体験談は1〜2枚に間引く**。目安通りに置くと物語が「資料」に見える。感情・論理の転換点（データ提示の直後など）を優先

## Step 5: 保存・完了報告

frontmatterは現行標準に従う：

```yaml
---
type: ai-work
status: review
created: YYYY-MM-DD
topic: 一言で何の記事か
promote: なし
---
```

- `promote` はnote体験談なら `なし` が既定。学び・技術が主役の記事になったときだけ昇格先の見当（Mind / Technical）を書く。

本文の下に `## メモ・経緯` を置き、リサーチの出典一覧・採用タイトルの診断スコア・画像プレースホルダの数を記録する。

完了報告では以下を必ず提示する：
- ファイルパス
- 採用したタイトル・書き出し（次点も1つ添える）
- 画像プレースホルダの数と場所（見出し名で）
- リサーチでスキップ/追加調査した箇所

## 禁止 / OK

- **禁止**：`Vault/` への書き込み／画像を自分で生成しようとする（プレースホルダに留める）／出典なしで数字・事実を断定する／listicleへの逃避
- **OK**：各Stageの手法は元Skillに準拠しつつ手順のみ内製（呼び出しはせず本Skill内で完結してよい）／事故（リサーチ材料が薄い等）があれば正直に報告して止まる

## 関連（手法を参照する元Skill）

- リサーチ手法：Step 1 に内蔵（旧 `research`、2026-07-27削除）
- 掴み手法：Step 2 に内蔵（旧 `hook-and-headline`、2026-07-17削除）
- 文体・黄金比率：`note-create`
- 図解プロンプトの流儀：`article-visual-planner`
